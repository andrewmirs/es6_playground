export function add(x, y){
    return x+y;
}

export const subtract = (x,y) => x-y;

export const multiply = (z,m) => z*m;

export const divide = (z,m) => m ? z/m : "can't divide by 0";

export default 'This is just a string right?';